
@model Shipment49Web.Common.GraphData
@{
    bool ropetailIsused = Shipment49Web.Common.CommonClass.GetIsRopeTail_Using();
}


<style>


    .modal-loader {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(255,255,255,0.7);
        display: flex;
        justify-content: center;
        align-items: center;
        z-index: 9999;
    }

    .modal-content {
        width: 100% !important;
    }

    .modal-lg .btn {
        color: white;
        background-color: White !important;
        border-color: White !important;
    }
	div#portModal {
    z-index: 9999;
   
}
div#portModal .modal-content {
    margin-top: 2% !important;
}
div#portModal .modal-lg {
    width: 100% !important;
     max-width: 1800px!important;
    max-height: 96vh !important;
    margin: auto!important;
    overflow: hidden;
    margin-top: 2%!important;
}
/* Let content decide height automatically */
div#portModal .modal-content {
    height: auto!important;
}

/* Scroll only inside modal body if content is large */
div#portModal .modal-body {
    max-height: 100%!important;
    overflow-y: auto!important;
}
div#portModal .modal-content {
    width: 100% !important;
    max-width: 1800px !important;
    max-height: 100% !important;
    margin: auto;
    overflow: hidden;
    margin-top: 2% !important;
}
.modal.in .modal-dialog {
    top: 0% !important;
}
div#portModal .box-body .table-responsive {
    min-height: .01%;
    overflow-x: auto;
    max-height: 100%!important;
}
.modal-content {
   
    margin-top: 0% !important;
}
div#portModal .modal-content {
    width: 100% !important;
 
    margin-top: 0% !important;
}

.box-body .table-responsive {
    min-height: .01%;
    overflow-x: auto;
    max-height: auto!important;
}
div#portModal .modal-content {
    width: 100% !important;
    height: auto !important;
    margin-top: 0% !important;
}
div#portModal .modal-body {
    max-height: 80vh!important;
    overflow-y: auto!important;
    /* width: 50%; */
}
div#portModal .modal-content {
    height: 93vh!important;
}
div#unsatisfactory .box-body .table-responsive {
    min-height: .01%;
    overflow-x: auto;
    max-height: 100%!important;
}
div#unsatisfactory .box-body .table-responsive {
    min-height: .01%;
    overflow-x: auto;
    max-height: auto!important;
}
div#portModal {
    padding-right: 0px!important;
}
.modal-open .modal {
    overflow-x: hidden;
    overflow-y: hidden!important;
}
div#ImageModal .modal-lg{
margin-top: 0% !important;
}
.chart-wrapper {
    width: 100%;
    overflow-x: auto;      /* horizontal scroll */
    overflow-y: auto;    /* no vertical scroll */
}

.chart-wrapper canvas {
    min-width: 600px;      /* keeps horizontal scroll working */
    width: auto !important;
    height: 60vh !important;   /* responsive height */
}
@@media (max-width: 768px) {
    .chart-wrapper canvas {
        height: 65vh !important;
    }
}
@@media (max-width: 768px) {
    
	h4.modal-title {
    text-align: left !important;
        font-size: 12px;
        font-weight: 600;
}
}
@@media (max-width: 395px) {
    
	h4.modal-title {
    text-align: left !important;
        font-size: 11px;
        font-weight: 600;
}
a.StopOnline {
    font-size: 11px;
}
}
div#portModal .modal-body {
    max-height: 80vh!important;
     overflow-y: auto!important;
    /* width: 50%; */
}
.table-responsive.table-striped {
    height: 70vh;
    overflow-y: scroll;
}
@@media (max-width: 1366px) {
div#ImageModal .modal-body {
    max-height: 78vh!important;
    overflow-y: auto!important;
    /* width: 50%; */
}
}
</style>


<div class="">
    @*<canvas id="@string.Format("myChart{0}", Model.chartId)" style="display: block;width: 300px;height: 200px;"></canvas>
        <input type="hidden" id="@string.Format("hdn{0}", Model.chartId)" class="hdn" value="@Model.data" />
        <input type="hidden" id="@string.Format("lbl{0}", Model.chartId)" class="hdn" value="@ViewBag.RunninghrsChartLables" />
        <input type="hidden" id="@string.Format("TTL{0}", Model.chartId)" class="hdn" value="@ViewBag.RunninghrsTitleHead" />*@

    <div class="chart-wrapper"> <canvas id="@string.Format("ports{0}", Model.chartId)" ></canvas></div>
    <input type="hidden" id="@string.Format("ports_hdn{0}", Model.chartId)" class="hdn" value="@Model.data" />
    <input type="hidden" id="@string.Format("ports_lbl{0}", Model.chartId)" class="hdn" value="@ViewBag.PortChartLables" />
    <input type="hidden" id="@string.Format("ports_TTL{0}", Model.chartId)" class="hdn" value="@ViewBag.PortTitleHead" />
    <input type="hidden" id="@string.Format("ports_ropetailIsused{0}", Model.chartId)" class="hdn" value="@ropetailIsused.ToString().ToLower()" />
    <hr />
</div>

<script>

    var currentPort = "";
    var currentRopeTail = "";

    var Titles = $("@string.Format("#ports_TTL{0}", Model.chartId)").val();
    var lbls = $("@string.Format("#ports_lbl{0}", Model.chartId)").val().replace(/\u200B/g, '');
    var data = $("@string.Format("#ports_hdn{0}", Model.chartId)").val();
    var ropetailIsused = $("@string.Format("#ports_ropetailIsused{0}", Model.chartId)").val() === 'true';
    //var arr=  @ViewBag.ChartLables;


    data = JSON.parse(data);
    console.log(data);

    // Filter out tail conditions where value is 0 and conditionally show/hide Tails dataset
    var labelsArray = lbls.split(',').filter(function(label) { return label.trim() !== ''; });
    var filteredLabels = [];
    var filteredData = [];

    // Find the "Tails" dataset
    var tailsDatasetIndex = -1;
    for (var i = 0; i < data.length; i++) {
        if (data[i].label && (data[i].label.toLowerCase() === 'tails' || data[i].label.toLowerCase() === 'tail')) {
            tailsDatasetIndex = i;
            break;
        }
    }

    // If ropetailIsused is false, remove Tails dataset completely but keep all items
    if (!ropetailIsused && tailsDatasetIndex !== -1) {
        // Remove Tails dataset from data array
        for (var m = 0; m < data.length; m++) {
            if (m !== tailsDatasetIndex) {
                filteredData.push(data[m]);
            }
        }
        filteredLabels = labelsArray;
    }
    // If ropetailIsused is true and Tails dataset exists, show ALL items (don't filter)
    else if (ropetailIsused && tailsDatasetIndex !== -1) {
        // Show all items regardless of Tails value - don't filter out any
        filteredLabels = labelsArray;
        filteredData = data;  // Use all datasets as-is
    } else {
        // If no Tails dataset found or condition not met, use original data
        filteredLabels = labelsArray;
        filteredData = data;
    }

    var ctx = $("@string.Format("#ports{0}", Model.chartId)");

    @*var maxval = @Model.chartId;*@

    // Calculate highest value from all datasets
    var highestValue = 0;

    filteredData.forEach(function (dataset) {
        dataset.data.forEach(function (val) {
            if (val > highestValue) {
                highestValue = val;
            }
        });
    });

    // Determine dynamic step size
    var stepSize;

    // Decide grid division based on maxval range
    if (highestValue > 1 && highestValue <= 10) {
        stepSize = 1;     // 2,4,6,8,10
    }
    else if (highestValue > 10 && highestValue <= 50) {
        stepSize = 5;    // 10,20,30,40,50
    }
    else if (highestValue > 50 && highestValue <= 100) {
        stepSize = 10;    // 20,40,60,80,100
    }
    else if (highestValue > 100 && highestValue <= 250) {
        stepSize = 20;    // 50,100,150,200,250
    }
    else if (highestValue > 250) {
        stepSize = 50;   // 100,200,300,400...
    }
    else {
        stepSize = 1;
    }

    var maxval = (Math.floor(highestValue / stepSize) * stepSize) + stepSize;

      var myChart = new Chart(ctx, {
        type: 'bar',
        data: {
            labels: filteredLabels,
            datasets: filteredData
        },
        options: {
              responsive: true,
              maintainAspectRatio: false,
            "hover": {
                "animationDuration": 0
            },
            "animation": {
                "duration": 1,
                "onComplete": function () {
                    var chartInstance = this.chart,
                        ctx = chartInstance.ctx;

                    ctx.font = Chart.helpers.fontString(
    valueFontSize,
    Chart.defaults.global.defaultFontStyle,
    Chart.defaults.global.defaultFontFamily
);
					//ctx.font = Chart.helpers.fontString(Chart.defaults.global.defaultFontSize, Chart.defaults.global.defaultFontStyle, Chart.defaults.global.defaultFontFamily);
                    ctx.textAlign = 'center';
                    ctx.textBaseline = 'bottom';

                    this.data.datasets.forEach(function (dataset, i) {
                        var meta = chartInstance.controller.getDatasetMeta(i);

                        // Only draw values if the dataset is visible
                        if (!meta.hidden) {
                            meta.data.forEach(function (bar, index) {
                                var data = dataset.data[index];
                                if (data > 0) {
                                    // Calculate label position - ensure it's always visible
                                    var chartArea = chartInstance.chartArea;
                                    var barHeight = chartArea.bottom - bar._model.y;

                                    // For very small bars (height less than 15 pixels), always show label above
                                    // For larger bars, show label 5 pixels above the bar
                                    var labelY;
                                    if (barHeight < 15) {
                                        // Small bars: position label higher to ensure visibility
                                        labelY = bar._model.y - 10;
                                        // But ensure it's not too close to top
                                        if (labelY < chartArea.top + 12) {
                                            labelY = Math.max(chartArea.top + 12, bar._model.y - 3);
                                        }
                                    } else {
                                        // Normal bars: standard positioning
                                        labelY = bar._model.y - 5;
                                        // Ensure label is never too close to top of chart
                                        if (labelY < chartArea.top + 15) {
                                            labelY = chartArea.top + 15;
                                        }
                                    }

                                    // Final safety check: ensure label is never below the bar
                                    if (labelY >= bar._model.y) {
                                        labelY = bar._model.y - 3;
                                    }

                                    // Draw the label - always visible for all non-zero values
                                    ctx.save();
ctx.fillStyle = '#333';
ctx.font = valueFontSize + "px Arial";   // 👈 FORCE exact size
ctx.textAlign = 'center';
ctx.textBaseline = 'bottom';
ctx.fillText(data, bar._model.x, labelY);
ctx.restore();
                                }
                            });
                        }
                    });
                }
            },
            title: {
                 display: true,
                 text: Titles,
                 fontSize: titleFontSize
            },
           legend: {
        display: true,
        align: 'left',
        labels: {
            fontSize: legendFontSize
        }
    },
            tooltips: {
                "enabled": true
            },
            scales: {
        yAxes: [{
            ticks: {
                fontSize: axisFontSize,
                max: maxval,
                beginAtZero: true,
                stepSize: stepSize
            }
        }],
        xAxes: [{
            ticks: {
                fontSize: axisFontSize
            }
        }]
    },

            onClick: function (evt) {

                var activePoint = this.getElementAtEvent(evt);

                if (activePoint.length > 0) {

                    var chart = this;

                    var firstPoint = activePoint[0];
                    var datasetIndex = firstPoint._datasetIndex;
                    var elementIndex = firstPoint._index;

                    var port = chart.data.labels[elementIndex];

                    // Get correct dataset label
                    var ropeTail = chart.data.datasets[datasetIndex].label;

                    if (ropeTail.toLowerCase() === "tails" || ropeTail.toLowerCase() === "tail") {
                        ropeTail = 1;
                    }
                    else {
                        ropeTail = 0;
                    }

                    currentPort = port;
                    currentRopeTail = ropeTail;

                    loadPortPage(1);

                }
            }

        }
    });

</script>


<script>


    function loadPortPage(pageNumber) {

        $("#portModal").modal('show')

        $("#portmodalLoader").show();

        var dateFrom = $("#TestDateFrom").val();

        var dateTo = $("#TestDateTo").val();


          $.ajax({
              url: '@Url.Action("GetDamageObservedGDetails", "Reports")',
              type: 'GET',
              data: {
                  Filter: currentPort,
                  RopeTail: currentRopeTail,
                  GType: "Port",
                  DateFrom: dateFrom,
                  DateTo: dateTo,
                  pn: pageNumber
              },
              success: function (response) {

                  $("#portmodalContentArea").html(response);

              },
              error: function () {
                  alert("Error loading data.");
              },
              complete: function () {

                  // 🔥 Hide loader after success OR error
                  $("#portmodalLoader").hide();
              }
          });
    }



</script>


<div class="modal fade" id="portModal" tabindex="-1">
    <div class="modal-dialog modal-lg" style="max-width:95%;">
        <div class="modal-content">

            <div class="modal-header">
                <h4 class="modal-title" style="text-align: center;">Port Wise Damage Observed</h4>
                <button type="button" class="close" style="margin-top: -20px; margin-right: 15px; opacity: 1;" data-dismiss="modal">&times;</button>

                @*<p style="float: right; margin-right: 30px;">
                     @Html.ActionLink("Download List", "DownloadDamageObservedGDetail")
                    </p>*@


                <div style="text-align: right;  margin-right: 52px; margin-top: -20px;">

                    <a href="@Url.Action("DownloadDamageObservedGDetail", "Reports")" class="StopOnline" title="Download Excel">
                       Download List
                    </a>

                </div>

            </div>

            <div class="modal-body position-relative" id="portModalBody">

                <!-- 🔥 LOADER -->
                <div id="portmodalLoader" class="modal-loader" style="display:none;">
                    <div class="spinner-border text-primary" role="status">
                        <span class="sr-only">Loading...</span>
                    </div>
                </div>

                <div id="portmodalContentArea">
                    Loading...
                </div>

            </div>

        </div>
    </div>
</div>


<style>
body.scroll-lock {
    position: fixed;
    width: 100%;
    overflow: hidden;
}
</style>

<script>

var scrollPosition = 0;
var modalCount = 0; // tracks how many modals are open

function lockScroll() {
    if (modalCount === 0) {
        scrollPosition = window.pageYOffset || document.documentElement.scrollTop;
        $('body').addClass('scroll-lock');
        $('body').css('top', -scrollPosition + 'px');
    }
    modalCount++;
}

function unlockScroll() {
    modalCount--;

    if (modalCount <= 0) {
        modalCount = 0;
        $('body').removeClass('scroll-lock');
        $('body').css('top', '');
        window.scrollTo(0, scrollPosition);
    }
}

// Apply to BOTH modals
$('#portModal, #ImageModal').on('show.bs.modal', function () {
    lockScroll();
});

$('#portModal, #ImageModal').on('hidden.bs.modal', function () {
    unlockScroll();
});

</script>